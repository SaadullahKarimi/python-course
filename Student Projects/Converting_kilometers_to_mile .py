# this project is designed for converting (KM) to (MILE)!
# First, it shows the following comment!
print("how  do you want to convert kms to mile")
# we generate a variable and store the following input!
kms=input()
# First, we generate a variable then any inputs is String. the following formual is for converting KM and MILE!
mile=float(kms)/0.621371
# there is a variable and round's function is counting the number of ashary!
print(f"ok kms convert to {round(mile,2)} mile")
